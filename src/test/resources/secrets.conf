jwt {
  access-token-secret: "access-token-secret"
  refresh-token-secret: "refresh-token-secret"
}

oauth-google {
  client-id: "client-id"
  client-secret: "client-secret"
}

mongodb-users {
  uri: "uri"
  database: "database"
  collection: "collection"
}

mongodb-refresh-tokens {
  uri: "uri"
  database: "database"
  collection: "collection"
}

mongodb-tab-group {
  uri: "uri"
  database: "database"
  collection: "collection"
}

mongodb-stash-setting {
  uri: "uri"
  database: "database"
  collection: "collection"
}

mongodb-counter {
  uri: "uri"
  database: "database"
  collection: "collection"
}

rabbitmq-server {
  host = "host"
  port = 8080
  virtual-host = "virtual-host"
  user = "user"
  password = "password"
}

rabbitmq-consumers {
  invalidate-refresh-tokens {
    queue: "InvalidateRefreshTokens"
    concurrent-consumers: 2
    prefetch-count: 2
  }
}
